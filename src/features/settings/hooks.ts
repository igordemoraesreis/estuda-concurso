// Stub temporário — a Task 9 substitui pela versão real (mesmo nome, mesma forma `{ data, isLoading }`).
export function useProfile(_userId: string | null) {
  return {
    data: null as null | { active_concurso_id: string | null },
    isLoading: false,
  };
}
